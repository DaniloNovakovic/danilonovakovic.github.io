# Asset Sources

This directory stores raw generated concept art and source ideation that should
stay in the repository but should not be copied into the production Vite build.

Use `public/assets/**` only for runtime-wired assets or prepared runtime
candidates that are intentionally deployable.
