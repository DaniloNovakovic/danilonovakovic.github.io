# Fallow baselines

Committed snapshots for incremental adoption. CI compares PR changes against these files so existing cleanup debt does not block merges.

Regenerate after intentional cleanup:

```bash
pnpm fallow:baseline
```
